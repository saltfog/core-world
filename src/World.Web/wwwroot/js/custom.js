(function ($) {

    jQuery(window).load(function() {
        //Show the loader
        //$(".spinner").show();

        $(href).load(this, function (result) {
            //Hide when complete
            $(".spinner").hide();
        });
    });

})(jQuery);