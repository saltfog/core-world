@model IEnumerable<World.Web.Models.SummaryPage>
@{
    ViewData["Title"] = "The World";

}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12-center">
    <table class="table table-striped-hover">
				<thead>
      <tr>
        <th>Countries</th>
        <th>Cities</th>
         <th>Population</th>               
      </tr>
    </thead>
    <tbody>
      @foreach (var u in Model.OrderBy(u => u.countries).ThenBy(u => u.cities).ThenBy(u => u.population))
        {
			<tr>		
            <td>
                   <strong>@u.countries </strong>
			</td>
		    <td>
                   <em> - @u.cities</em>
           </td>
                        <td>
                   <em> @u.population</em>
           </td>
		  </tr>
        }
    </tbody>
  </table>
    </div>
</div>

